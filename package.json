{
    "name": "my-static-site",
    "version": "1.0.0",
    "scripts": {
      "build": "echo 'No build step required for static site'"
    },
    "vercel": {
      "builds": [
        {
          "src": "/*",
          "use": "@vercel/static"
        }
      ]
    }
  }